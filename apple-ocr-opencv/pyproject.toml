[project]
name = "apple-ocr-opencv"
version = "0.1.0"
description = "Cell-based OCR using Apple Vision + OpenCV table detection"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.2",
    "ocrmac>=1.0.1",
    "opencv-python-headless>=4.13.0.92",
    "pillow>=12.1.1",
]
